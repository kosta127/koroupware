<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="kosta.login.mapper.SignUpMapper">
	<cache />
		<insert id="SignUp" parameterType="Emp">
		insert into EMP(
			emp_no, emp_name, dept_no, emp_email, emp_id, emp_password, emp_address, emp_approvalyn, emp_residentnumber, emp_hiredate, emp_img, emp_elec_auth_img, emp_superior, emp_reporter, position_no, office_no) 
			values( 
			#{emp_no}, #{emp_name}, 0, #{emp_email}, #{emp_id}, #{emp_password}, #{emp_address}, 'N', #{emp_residentnumber}, SYSDATE, #{emp_img}, #{emp_elec_auth_img}, 0, 0, 0, 0) 
		</insert>
		<insert id="TelInsert" parameterType="Tel">
		insert into TEL(
			tel_no, emp_no, tel_type, tel_telephone) 
		values( 
		#{tel_no}, #{emp_no}, #{tel_type}, #{tel_telephone})
		</insert>
		
	<select id="selectEmp_no" resultType="Integer">
		select max(emp_no) from EMP
	</select>
	
	<select id="selectTel_no" resultType="Integer">
		select max(tel_no) from TEL
	</select>
	
</mapper>
